# Prints basic information about a `mlpsa` class.

Prints basic information about a `mlpsa` class.

## Usage

``` r
# S3 method for class 'mlpsa'
print(x, ...)
```

## Arguments

- x:

  the `mlpsa` class.

- ...:

  unused.
